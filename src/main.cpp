#include <node.h>
#include "sdk.h"

namespace {
  using namespace v8;
  using namespace node;

  class TeraInternals {
    TArray<UObjectEx*> *GObjects = NULL;
    TArray<FNameEntry*> *GNames = NULL;

    TeraInternals() {
      return;
    }

    void get() {
      return;
    }
    
  };
  
}
